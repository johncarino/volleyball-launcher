- Going back to configure from advanced stops the machine
- Move JS to separate file

- in launch mode, the set slot that is being executed should be highlighted in some way
- in launch mode, the back button may be too small, a little unintuitive perhaps
- advanced mode i think makes more sense to be an option in launch mode rather than in configuration mode
- in config mode, have some sort of information visual or word that tempos 1 to 5 corresponds to low arc to high arc (0.5m difference between each tempo)
- in config mode, the user should not be able to save sets from different machine positions at once (ie. set slot 1 with L position, set slot 2 with R position)
- the optimal solution is to have 4 slots available for L and R each and saved sets memorized throughout a session
- maybe change background colour between config mode and launch mode to indicate that the mode changed
- further on, we would a countdown timer on the launch screen before each launch (maybe overlayed on top of the court visual only when counting down)